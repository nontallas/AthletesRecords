package controller;

public class Controller implements Command{

// TODO
    /*
    *
    * Create case for every different select
    * Check in every case if there's already an object
    * Make selects safe against injections && complete them with where
    * Create UI
    *
    */
    @Override
    public void execute() {

    }
}
